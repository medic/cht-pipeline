version: 1

models:
  - name: contact
    columns:
      - name: uuid
        constraints:
          - type: unique
        tests:
          - not_null
  - name: person
    config:
      contract:
        enforced: true
    columns:
      - name: uuid
        data_type: string
        tests:
          - not_null
  - name: place
    columns:
      - name: uuid
        tests:
          - not_null
