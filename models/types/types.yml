version: 2

models:
  - name: data_record
    tests:
      - dbt_utils.fewer_rows_than:
          compare_model: ref('couchdb')
  - name: person
    tests:
      - dbt_utils.fewer_rows_than:
          compare_model: ref('couchdb')
