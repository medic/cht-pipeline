version: 2

models:
  - name: couchdb
    columns:
      - name: _id
        tests:
          - not_null
      - name: _rev
        tests:
          - not_null
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - _id
            - _rev
